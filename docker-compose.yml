services:
  agents:
    build: .
    command: python main.py
    env_file: .env
    volumes:
      - db_data:/data
    environment:
      - DB_PATH=/data/database.db
    restart: unless-stopped

  web:
    build: .
    command: python app.py
    env_file: .env
    volumes:
      - db_data:/data
    environment:
      - DB_PATH=/data/database.db
    ports:
      - "5000:5000"
    restart: unless-stopped
    depends_on:
      - agents

volumes:
  db_data:
